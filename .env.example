# Copy this file to ".env" (same folder) and fill in your key.
# ".env" is gitignored, so it is never committed or uploaded.
#
#   cp .env.example .env   # then edit .env
#
# Free FEC key: https://api.data.gov/signup/  (used by scripts/build-contributions.mjs)
FEC_API_KEY=your_key_here
